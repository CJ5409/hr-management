# HR Management System

A full-stack HR management application with a React frontend and Node.js backend.

## Setup

1. **Install Dependencies**:
   - Client: `cd client && npm install`
   - Server: `cd server && npm install`

2. **Start MongoDB**: